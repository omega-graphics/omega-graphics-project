// §2a follow-up — `setMeshOutputs(nv, np)` is mesh-stage-only (mirrors the
// barriers-only-in-compute rule). Calling it from a compute shader must be
// rejected by Sema.

compute(x=1, y=1, z=1)
void main(uint3 tid : GlobalThreadID){
    setMeshOutputs(1u, 1u);
}
