Review .tasks/*.md files and update them based on work completed this session.

For each active task file (Status: Not Started or In Progress):
1. Check each `- [ ]` item against the current codebase state
2. Mark completed items as `- [x]`
3. Update **Status** field:
   - `In Progress` — some items done
   - `Complete` — all items done
4. Mark newly blocked items with `**BLOCKED**: reason`
5. If scope changed during implementation, update task descriptions to match
6. Completed tasks should be archived in the `archive/` directory

Only modify .tasks/*.md files. Do not make code changes.
